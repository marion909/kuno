{
  "name": "@kuno/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and utilities for Kuno messaging platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@signalapp/libsignal-client": "^0.55.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "rimraf": "^5.0.5"
  }
}
